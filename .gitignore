node_modules
dist
out
.DS_Store
.eslintcache
*.log*
.env
.env.*

# Google OAuth client (desktop). Keep local; pack from resources at build time.
resources/google-oauth.json

# Agent throwaway scripts/research (only scratch dir allowed)
.agent-scratch/
.user-scratch/
.user-scratch-*/

# Local Electron user-data for extra instances (e.g. bun run dev:peer)
.userdata/
.userdata-*/
samples/

